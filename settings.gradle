rootProject.name = 'davesgame'
include 'domain'
include 'authenticate'
include 'rest'
include 'server'
rootProject.children.each { it.name = rootProject.name + '-' + it.name }
include 'message-generator'
