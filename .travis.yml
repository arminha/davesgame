language: java
jdk:
  - openjdk7
  - oraclejdk8

env:
  - TERM=dumb

# use docker infrastructure
sudo: false

# caching
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
